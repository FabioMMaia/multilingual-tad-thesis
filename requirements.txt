# Core scientific stack
numpy>=0.16.0
pandas>=1.0.0
scikit-learn>=1.3.2
scipy>=1.2.0
tqdm>=4.66.1

# Deep learning
torch>=2.5.0
accelerate>=0.21.0
safetensors>=0.3.1

# Sentence encoders & SetFit
# Pinned versions — tested in paper experiments
transformers==4.40.2
sentence-transformers==2.6.1
setfit==0.7.0
datasets==2.18.0
fsspec==2023.6.0
huggingface_hub>=0.14.0

# Anomaly detection models
pyod
deepod

# Dataset loading
kagglehub>=0.3.3
mwparserfromhell        # WikiNews dump parser

# Utilities
python-dotenv
unidecode
seaborn
matplotlib>=3.0.0

# ─────────────────────────────────────────────────────────────────────────────
# LLM backends — install only what you use
# ─────────────────────────────────────────────────────────────────────────────


# Google Gemini API (free tier may be geo-restricted in some regions)
# google-genai

# OpenAI API
# openai

# Local GGUF inference via llama-cpp-python (no API key, no token limits)
# CPU-only:   pip install llama-cpp-python
# GPU/CUDA:   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --no-cache-dir
